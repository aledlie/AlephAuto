id: query-builders
language: javascript
rule:
  any:
    - pattern: $DB.query($$$)
    - pattern: $DB.execute($$$)
    - pattern: $CONN.query($$$)
    - pattern: SELECT * FROM $$$
    - pattern: INSERT INTO $$$
    - pattern: UPDATE $TABLE SET $$$
    - pattern: DELETE FROM $$$
message: Database query pattern
severity: info
note: Raw query patterns that could benefit from query builder abstraction
